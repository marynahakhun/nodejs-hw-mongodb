description: Unauthorized error
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 404
        message:
          type: string
          example: UnauthorizedError
        data:
          type: object
          properties:
            message:
              type: string
              example: 'Not found'